# Host directory holding the reth database, the lighthouse database and the
# shared engine-API JWT. Must be an absolute path — docker compose does not
# expand ~ or $HOME here. `./deploy/setup.sh` writes a real deploy/.env for you.
DATA_DIR=/home/youruser/reth-ext-data
