[package]
name = "exex-proto"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
repository = "https://github.com/opcode-fi/reth-ext"
description = "Protocol buffer definitions for remote ExEx gRPC service"

[dependencies]
tonic = "0.11"
prost = "0.12"

[build-dependencies]
tonic-build = "0.11"
